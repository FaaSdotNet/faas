@model UserViewModel
@{
    ViewData["Title"] = "Home Page";
}


<div class="row">
    <div class="col-lg-12">
        @if (Model == null)
        {
            <h3>Welcome to Form as a Service!</h3>
        }
        else
        {
            <h3>Hi @Model.DisplayName!</h3>
            <h3>Welcome to Form as a Service!</h3>
        }
    </div>
</div>
